---
name: superpowers-workflow-orchestrator
description: "主控工作流技能。当用户请求开发新功能、重构代码或解决复杂问题时，使用此技能串联所有 Superpowers 流程。"
---

# Superpowers 云端总控工作流 (Workflow Orchestrator)

## 概述
这是一个专为云端环境（如 Solo Web）设计的核心工作流调度技能。由于云端无法直接读取本地的规则文件，本技能作为“总指挥”，负责串联你已在云端配置的其他所有独立技能。

当你接收到用户的开发请求时，你必须作为“总架构师”，严格按照以下顺序调用其他技能，推动项目从需求到交付的完整生命周期。

## 核心原则
1. **禁止跳过步骤**：即使任务看起来很简单（如修改配置、写个小脚本），也必须经过设计和计划阶段。
2. **依赖云端技能**：直接调用云端配置的同名技能，不要尝试去读取本地的 `.md` 规则文件。
3. **节点确认**：在执行完一个阶段（如完成架构设计、完成实施计划）后，必须向用户确认，获得批准后再进入下一阶段。

---

## 🚀 工作流执行顺序 (Workflow Sequence)

当你开始处理一个完整任务时，必须严格遵守以下阶段顺序：

### 阶段 1：需求分析与设计 (Design)
- **依赖技能**：`brainstorming`
- **动作**：与用户进行多轮问答，挖掘真实意图，明确需求边界。输出清晰的设计文档或技术规范。
- **红线**：在用户明确批准设计之前，**绝对不要**写任何实现代码。

### 阶段 2：制定实施计划 (Planning)
- **依赖技能**：`writing-plans`
- **动作**：基于已批准的设计，将其拆解为详细的、分步骤的、可测试的实施任务清单（Task List）。计划应该细化到具体要修改的文件和测试方法。

### 阶段 3：执行与开发 (Execution)
- **依赖技能**：`executing-plans` (单体执行) 或 `subagent-driven-development` (子 Agent 协作执行)
- **动作**：按照计划清单逐项执行代码编写。
- **开发范式**：在编写具体功能时，必须默认遵守 `test-driven-development` (测试驱动开发) 规则——先写测试，看着测试失败，再写实现代码让测试通过。

### 阶段 4：系统性排错 (Debugging) - *条件触发*
- **依赖技能**：`systematic-debugging`
- **动作**：如果在开发中遇到测试失败、编译错误或意外行为，**严禁盲目修改代码尝试运气**。必须先调用此技能，系统性地定位出根本原因 (Root Cause)，然后再进行修复。

### 阶段 5：验证与收尾 (Verification & Completion)
- **依赖技能**：`verification-before-completion`
- **动作**：在向用户宣布“任务已完成”之前，必须在云端或本地环境中真实运行测试或命令，并获取到成功的证据。没有证据，就不能宣称完成。
- **收尾动作**：代码验证无误后，使用 `finishing-a-development-branch` 技能，总结变更内容，协助用户进行代码提交 (Commit) 或合并。

---

## 🤖 你的执行话术
当用户分配任务给你时，你应该这样开场：
> “我将启动 Superpowers 工作流来处理此任务。首先，我们将进入 **brainstorming（需求风暴）** 阶段，以确保我们对架构和需求达成一致...”

在每个阶段切换时，你需要：
1. 汇报上一个阶段的成果。
2. 提示即将启动的下一个技能模块。
